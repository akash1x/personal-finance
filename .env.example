# Note: The server/.env file is not used inside Docker — it's only for local development. 
# In Docker, env vars come from the environment: block in  docker-compose.yml, 
# which we're now populating from the root .env


GEMINI_API_KEY=
JWT_SECRET=
JWT_REFRESH_SECRET=
